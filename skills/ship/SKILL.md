---
description: The go-live step, whatever live means here: gates green, release notes, tag, announce, ledger.
---

Load and follow the **multica-ops** skill (`../mops/SKILL.md`), executing
its `/multica-ops:ship` flow. The go-live step, whatever 'live' means: deploy code, launch a batch, publish an episode. Gates green → release → notes → tag → announce (outward → owner-confirmed); writes the cost/effort ledger; marks shipped in ROADMAP. Args: $ARGUMENTS
