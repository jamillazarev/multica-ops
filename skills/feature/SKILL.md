---
description: Add a feature mid-flight — grilled into a spec, ICE-scored against the backlog, slotted into a release.
---

Load and follow the **multica-ops** skill (`../mops/SKILL.md`), executing
its `/multica-ops:feature` flow. Add a feature mid-flight: raw description is fine — the conductor grills, writes the spec, ICE-scores vs the backlog, proposes a release slot, waits for approval. Args: $ARGUMENTS
