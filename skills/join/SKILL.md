---
description: Take over an existing workspace — audited before anything is touched, then fixed in batches you approve.
---

Load and follow the **multica-ops** skill (`../mops/SKILL.md`), executing
its `/multica-ops:join` flow. Join an existing Multica setup: /multica-ops:audit under the hood + interview delta (incl. members, brand & design system, governance); reconcile an existing Mops agent, never duplicate.
