---
description: Advisory session — answer a question as an advisor, zero standing footprint (default addressee Mops; may address any agent, expert or the theatre); research and requested validation run ephemerally; projectize only if the answer leads there.
---

Load and follow the **multica-ops** skill (SKILL.md), executing its `/mops consult` flow
(FLOWS.md → Consult). An advisory session: answer the owner's question as an advisor with a
**zero standing footprint** — create nothing that outlives the session, keep answers sourced
and evidence-tiered, spin research (and, on request, validation) sub-agents ephemerally, and
offer to persist or projectize only when the answer leads there.

**Optional addressee** — `/mops consult [who] <question>`: the contract is a property of the
conversation, so the default is **Mops** but any addressee answers under it. Name a **team agent**
(answers from its own craft), an **expert** (its own voice and sources), or the **theatre**
(personas react — direction-only, 🎭-marked, registered nowhere). Route to the **one** addressee,
no fan-out. Args: $ARGUMENTS
