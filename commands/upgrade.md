---
description: Update skills safely: dry-run impact report → commit current to docs/skill-backups/ (git = history, SHA in UPGRADES.md) → apply → reconcile dependents → verify/rollback.
---

Load and follow the **multica-ops** skill (SKILL.md), executing
its `/mops upgrade` flow. Update skills safely: dry-run impact report → commit current to docs/skill-backups/ (git = history, SHA in UPGRADES.md) → apply → reconcile dependents → verify/rollback. Args: $ARGUMENTS
