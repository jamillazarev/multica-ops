---
description: The go-live step, whatever 'live' means: deploy code, launch a batch, publish an episode. Gates green → release → notes → tag → announce (outward → owner-confirmed); writes the cost/effort ledger; marks shipped in ROADMAP.
---

Load and follow the **multica-ops** skill (SKILL.md), executing
its `/ship` flow. The go-live step, whatever 'live' means: deploy code, launch a batch, publish an episode. Gates green → release → notes → tag → announce (outward → owner-confirmed); writes the cost/effort ledger; marks shipped in ROADMAP. Args: $ARGUMENTS
