---
description: Set or show the budget envelope (multica-ops /budget): amount + period, currency, credits and grants with expiries; reports burn, runway and cost per shipped feature.
---

Load and follow the **multica-ops** skill (SKILL.md), executing its `/budget` flow. Set or
show the envelope: amount + period, currency (USD default), credits/grants with expiries,
what counts; report burn, runway and cost per shipped feature, and re-propose the stack if
the number changes. Args: $ARGUMENTS
