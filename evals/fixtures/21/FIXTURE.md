Scenario 21 — see evals/README.md for the assertions.

Repository half only. Where a scenario needs workspace state as well, its builder lives in
`scripts/eval-fixture.py`; where the situation cannot be built at all, the run record says so
rather than scoring a fixture that encodes something else.
