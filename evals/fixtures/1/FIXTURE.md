Scenario 1 — "a small job, not a company".

A one-page site with three buttons and a deploy-on-push README. Deliberately NOT a company:
no board, no docs skeleton, no `_ops/`. The scenario asks whether the skill can leave it that
way and still ask before pushing, so the fixture must give the job a real place to happen and
nothing else.
