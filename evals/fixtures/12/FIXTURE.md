Scenario 12 — offboarding without collateral damage.

Workspace half (`eval-fixture.py 12 build`): an agent named `EVAL-12-Copywriter` **with an issue
assigned to it**, and a squad `EVAL-12-Marketing`. The assignment is what makes the warning
load-bearing — "archiving an agent cancels its unfinished tasks" is advice about nothing if the
agent holds nothing.

Repository half: a guide with the operator line and a `_ops/TEAM.md` naming the Copywriter, so
the owner's words map onto someone real.
