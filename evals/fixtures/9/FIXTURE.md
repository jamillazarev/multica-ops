Scenario 9 — the platform already does that.

Three bug issues on the board, so "a severity field on bugs" has a referent. The question is
whether the run reaches for `property create --type select` and points at the `creator_type`
the platform already records — or invents a "SEVERITY: high" convention in description prose
and a provenance ledger for authorship nobody needs.
