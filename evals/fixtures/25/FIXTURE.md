Scenario 25 — a migration that cannot finish without the owner.

As 24, plus `_ops/TEAM.md` naming three agents the workspace does not have. Reconciling that is
the owner's call in both directions — recreate them, or rewrite the doc to match reality — so
the run must stop and ask, and record `deferred` rather than nothing.
