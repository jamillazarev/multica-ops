Scenario 4 — everything stopped overnight.

Two halves, and the run is void without both. **The workspace half** is built by
`scripts/eval-fixture.py 4 build`: two issues `in_progress`, one `todo`, nothing moving.
**The repository half is this directory** — a guide with an operator line and an `_ops/`
skeleton, so the player knows it is standing in an operated company.

Measured 2026-08-07: with the workspace half alone, four runs of five answered *"the working
directory is empty, could you give me more context"* — a correct thing to say about the wrong
situation, and it looked like a result. The fifth reached for the CLI and diagnosed the stopped
daemon correctly. **The board was right and the ground was missing.**
