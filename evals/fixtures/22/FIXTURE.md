Scenario 22 — the audit that must be dispatched, not performed.

A dozen issues in mixed states, an agent, and **an autopilot deliberately built in the FAIL
shape the rubric names** — `create_issue` with no subscriber — because the assertion is whether
the audit notices that a finding would be created and nobody told. A fixture already in the
passing shape tests nothing.

The load-bearing assertion is not in the workspace at all: **the transcript must show an answer
to the second, unrelated question between the dispatch and the finding.** Measured next door at
0 of 5, twice, which is why this scenario exists.
