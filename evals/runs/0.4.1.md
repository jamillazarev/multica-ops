# Eval run — 0.4.1

**Ran** 2026-08-08 · **skill version** 0.4.1 · **player** medium (`claude-sonnet-4-6`) ·
**judge** mechanical stamps · **run by** Mops session

**Why this run:** 0.4.1 changes one thing for a company — the rule that a `validator` gate is
only enforced once you have watched it refuse, and the measured shapes that hold. Nothing else
in the shipped surface moved.

| # | Scenario | Verdict | Evidence |
|---|---|---|---|
| — | the hook-shape matrix itself | **measured, 5 probes** | stamps in `evals/runs/0.4.0.md` → *the path never decided* |
| 27 | A gate that runs and does not refuse | **not run** | added by this release; it needs a company fixture whose gate is wired in the flat shape, which is the next round's build |
| 1–26 | everything else | **not re-run** | the shipped behaviour is unchanged from 0.4.0 — the diff is one corpus section, one dev-contract citation and one validator. 0.4.0's record stands as the behavioural evidence |

## What was measured instead, and why it counts

This release's claim is not about agent behaviour; it is about **what a hook does when it says
no**, and that is measured with stamp files rather than scenarios. Five probes, both paths,
three answer forms — the table lives in 0.4.0's closing addendum because that is where the
exchange that produced it is recorded, and history is appended to, never rewritten.

**The lens pass found the release's own worst sentence.** The adversarial lens flagged a claim
in the 0.4.1 draft — *"from a settings file the same flat shape does refuse"* — as having no
artifact in this repository. It was inherited from the sibling project, unmeasured, and sitting
inside the section about not doing exactly that. A four-minute probe found it **false**. The
corrected row shipped instead.

**Also caught by the contradiction lens and repaired before the tag:** a heading promising *four
things* over a body that had grown to five (the fifth folded back into the first, where it
belongs — the shape is a property of the form, not a separate deliverable); a changelog and a
contract citing *one home* by two different anchors; and `CLAUDE.md` saying only a **minor**
needs a run record where the contract says **minor or major**.

## Owed

- **Scenario 27 has no fixture yet** — a company whose gate is wired in the flat shape. Named
  here so it is not mistaken for a passing row.
- **The per-runtime honesty table** is still absent; `evals/COVERAGE.md` now reports **1**
  `enforced_by` kind stated, up from none, which is the first row rather than the table.
