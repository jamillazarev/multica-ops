# Eval run — 0.2.1

**Ran** — · **skill version** 0.2.1 · **verdict: reuses `0.2.0.md`, and says so.**

**Why this run reuses the previous record:** 0.2.1 is a patch whose whole content is a
*correction to a description* — what the app does with an attachment, and what a release row is
titled. It changes no flow, no gate and no interview, and **no scenario in the rubric exercises
attachment rendering**, so there is nothing here a run could have caught that the 0.2.0 record
does not already say was unmeasured.

**The debt is unchanged and still owed**: 22 scenarios, never executed once. `0.2.0.md` names the
cheaper next move — a stratified subset at full N (1, 4, 10, 22) rather than all 22 at N=1.

**Noticed, not asked:** the defect this patch fixes was not a behaviour, it was a **claim written
from a header instead of from the thing** — an HTTP `content_type` read as if it answered "what
does the reader see". No eval scenario can catch that class; the check that would is the four
lenses, and specifically the adversarial one asking *what is this claim actually evidence of*.
Worth a scenario only if it recurs.
